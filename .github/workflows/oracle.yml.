name: AIRI Oracle

on:
  push:
    branches: [ main, ai-fixes ]
  pull_request:
    branches: [ main ]

jobs:
  oracle:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Oracle Requirements
        run: |
          pip install -r tools/airi_oracle/requirements.txt

      - name: Run AIRI Oracle
        run: |
          python tools/airi_oracle/oracle.py

      - name: Upload Oracle Report
        uses: actions/upload-artifact@v4
        with:
          name: oracle-report
          path: oracle_report.json
