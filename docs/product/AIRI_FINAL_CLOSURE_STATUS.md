# AIRI Final Closure Status

**Branch:** `cp-foundation`

**Program status:** `IN_PROGRESS`

**Release-priority authority:** [`AIRI_RELEASE_CLOSURE.md`](AIRI_RELEASE_CLOSURE.md)

**Technical execution trace:** [`AIRI_FINAL_CLOSURE_MAP.md`](AIRI_FINAL_CLOSURE_MAP.md)

## Current milestone

| Milestone | Status | Evidence | Remaining boundary |
|---|---|---|---|
| Closure-map baseline | `IMPLEMENTED` | `AIRI_FINAL_CLOSURE_MAP.md` maps P0/P1/P2 ownership, runtime, persistence, security, UI, tests and external gates. | Map must be updated only when a real runtime or acceptance gate changes. |
| Project Context admission | `IMPLEMENTED` / `TESTED` | `ProjectContextResolver`, scoped `RagRetriever`, `ProjectContextAdmissionPolicyTest`, core verifier. | Device/model execution and broad cross-resource isolation. |
| Project Home derived overview | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` | `WorkspaceContext` now derives active-project file/artifact/task counts, latest task, unexpired pending approvals, and one non-executing next action from canonical stores; `WorkspaceScreen` opens the existing Task, Library, or Chat surface. `WorkspaceContextTest`, Kotlin compilation, 67/67 core checks, and strict four-locale parity passed. | Verify actual project switching, no-data/loading/error states, TalkBack, RTL/LTR, large fonts, touch targets, and navigation return on a physical device/emulator. It is not a durable plan board or an execution engine. |
| Explicit response preferences | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` | `EXPLICIT_RESPONSE_PREFERENCES_CONTRACT.md`; two bounded user-entered fields remain app-private and enter `PromptService` only after `shareWithResponses` opt-in, marked as data rather than instructions. `UserPreferenceProfileTest`, Kotlin compilation, 68/68 core checks, and strict four-locale parity passed. | Verify persistence, opt-in/off behavior, prompt construction and response suitability, TalkBack, font scale, backup/account deletion implications, and local/cloud privacy behavior on a device. No inferred age/profession, memory admission, authorization, or general profiling is implemented. |
| Chat session lifecycle actions | `IMPLEMENTED` / `BUILD_VERIFIED` | History now opens a selected session's persistent actions for Pin/Unpin, Rename, and confirmed Delete. `ChatViewModel` routes the selected session ID through canonical `MemoryManager` operations; the list already orders pins first. Kotlin compilation and 69/69 core checks passed. | Run Room/session persistence, rename validation, pin ordering, delete/current-session replacement, TalkBack, RTL/LTR, and long-press/menu navigation on a device. Archive, sharing with explicit redaction/confirmation, session-to-project linking, and bulk data export remain separate work. |
| Trust Center | `IMPLEMENTED` / `TESTED` | `TRUST_CENTER_CONTRACT.md`, live governance/durable approval bridge. | Exact-step continuation for AgentLoop, skills, terminal, and remaining connector paths; device recovery. |
| Mission ownership baseline | `IMPLEMENTED` / `TESTED` | `MissionKernel`, normalized `DurableTaskManager` persistence, `MissionKernelTest`, and `DurableTaskProductKernelTest`. | Broader cross-resource integration remains separate P0 work. |
| Project secret broker + GitHub consumer/management | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` | Project/connector-scoped capability namespace in `SecretVault`, regression coverage for cross-project denial/revocation/presence, active-workspace GitHub credential save/revoke UI that exposes no raw preview/copy, and a GitHub adapter path that validates persisted task/run/step ownership before consuming one project-bound `GITHUB_PAT` capability internally. `GitHubConnectorProjectSecretTest` injects ownership/transport and proves missing-secret or ownership mismatch never reads a legacy credential or calls the adapter, while an owned project path uses only scoped material without HTTP. | Provider adapters beyond GitHub, live GitHub/Keystore/workspace-switch verification, capability revocation during provider I/O, and accessible UI testing remain open. |
| Exact-step GitHub continuation | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` | `APPROVAL_CONTINUATION_CONTRACT.md`; durable PAUSED/CLAIMED states, restart sweep, Trust Center allow route, claimed connector authorization; `DurableTaskProductKernelTest` + `ConnectorRuntimeManagerTest` passed. | Migrate AgentLoop, skills, terminal, and remaining side-effecting connectors; validate a credentialed GitHub mutation and device process recovery. |
| Artifact execution provenance + evidence UI | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` | `ARTIFACT_PROVENANCE_CONTRACT.md`; Room v9 migration/schema, project/task/run/step/tool/model/hash metadata, project-scoped reads, orchestrator result route, and Library rendering of bounded execution evidence/integrity prefix; targeted tests passed. | Migrate every remaining producer, run instrumentation migration, and verify accessibility, preview/share/download plus multi-step provider execution on device. |
| Cross-project file/knowledge/memory/secret/artifact fixture | `BUILD_VERIFIED` | `ProjectResourceIsolationTest` compiles against real ProjectFileManager, ProjectKnowledgeManager, MemoryManager, SecretVault, and ArtifactManager paths; it asserts A/B file visibility, knowledge search, project-scoped memory recall, project-secret denial/consumption, soft-delete/restore/reindex, and artifact lookup/read denial. | Execute the instrumentation fixture on a physical device/emulator. |
| Project file lifecycle | `IMPLEMENTED` / `BUILD_VERIFIED` | `PROJECT_FILE_INTELLIGENCE_CONTRACT.md`; project-owned private trash, restore through managed storage, explicit purge, knowledge cleanup, Library recovery UI, and account filesystem wipe. | Run picker/media/delete/restore/purge sequence on a device; add rich parser and content-version coverage. |
| Memory management UI and retrieval | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` | `MEMORY_FABRIC_CONTRACT.md`; MemoryScreen explains scope/provenance, confirms deletion, and edits content only for durable long-term rows through ChatViewModel. `MemoryManager.editMemoryContent` preserves metadata. Live RAG ranks already-authorized semantic and project-knowledge passages by bounded retrieval score, then appends durable memory in governed DAO order because durable confidence is not query relevance; exact normalized-content duplicates are removed without changing scope/privacy or inferring truth. `RagRetrievalRankerTest` passes in JVM; the Room edit fixture compiles and four-locale parity is strict. | Run `MemoryDaoInsertIdTest.editingLongTermMemoryChangesContentAndPreservesScopedMetadata` on a device/emulator; verify retrieval under real embedding/project indexes, edit/delete dialogs, TalkBack, font scale, and post-restart refresh. Taxonomy, ranking calibration/decay, contradiction handling, and user export remain open. |
| Local browser handoff boundary | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` | `BROWSER_AND_ARTIFACT_CONTRACT.md`; `LocalBrowserOperator` resolves only public browser candidates, routes HTTP(S) through `BrowserNavigationPolicy`, and emits a process-local coordinator request rather than launching an external activity. `AiriApp` displays the normalized public URL and requires a visible **Open browser** choice; the coordinator revalidates policy and calls `ACTION_VIEW` once only after that choice, handling missing/denied Android handlers safely. Coordinator, local-policy, and navigation-policy JVM tests plus core guard and strict localization passed. | Verify browser selection, cancellation, missing-handler feedback, configuration/process recreation, TalkBack, and font scale on a device. Authenticated browsing, DOM actions, upload/download flows, `geo:` app launch, and durable browser side-effect approvals remain open. |
| Project file edit proposal / approval / apply | `IMPLEMENTED` / `TESTED` / `CI_VERIFIED` / `RUNTIME_VERIFICATION_PENDING` | `PROJECT_FILE_EDITING_CONTRACT.md`; `ProjectFileEditRuntime` persists private proposals, uses a hash-only typed continuation, pauses/claims exact task/run/step, invokes only project-scoped `ProjectFileManager` revision storage, clears stale knowledge, rolls back when evidence cannot link, and sends bounded evidence to the durable timeline. Library provides user-authored text and diff review before Trust Center approval. It now requires an explicit task choice rather than taking the first stored task when a project has multiple eligible running tasks; the pure selector is JVM-tested for unique selection and ambiguous refusal. `ProjectFileApprovalRecoveryTest` now creates the managed file and owned running task, then reconstructs the task/workspace/file/artifact/proposal runtimes after either decision. The approved branch applies once, verifies project/task/run/step artifact provenance, and refuses a second recovery resume; the denied branch changes no bytes, creates no artifact, and cannot resume. GitHub Actions run `32695492715` for `73ac96ad`, run `32697399760` for `bfef9bb3`, and run `32702865271` for `6d78e0d0` passed contracts, strict localization, debug/unit/lint/release-source, `connectedDebugAndroidTest`, and native-library verification. | Verify approval rejection and UI-driven allow/deny/replay, deliberate physical process death, storage failure rollback, TalkBack, font scale, and large/read-only file behavior on a device. CI emulator evidence does not prove a signed artifact or store/provider acceptance. |
| AgentLoop side-effect boundary | `IMPLEMENTED` / `TESTED` | `AGENT_LOOP_DURABLE_APPROVAL_CONTRACT.md`; `AgentLoopSideEffectPolicy` remains fail-closed for notes, alarms, app launch, accessibility actions, skills, browser, terminal, and connector mutation. Read/conversation tools remain outside the write gate, and `ask_confirmation` remains an in-memory conversational sentinel only. | Migrate each remaining side-effect family with its own typed contract; do not treat a calendar context or a conversational confirmation as authorization for any other operation. |
| Scheduled-task evidence UI | `IMPLEMENTED` / `BUILD_VERIFIED` | `SCHEDULED_AUTOMATION_CONTRACT.md`; Agent Tasks retains persisted completed/failed jobs, refreshes explicitly without polling, focuses `lastDurableTaskId` exactly in Execution Center, and offers a visible confirmation for one immediate agent-job execution. `runNow` persists a distinct manual WorkManager request without replacing the schedule, prevents duplicate active manual runs, refuses maintenance jobs, and clears its marker only from the matching worker result. | Validate WorkManager timing, manual-run enqueue/completion/duplicate refusal, outcome refresh, exact linked-task focus/return, Doze/OEM behavior, and accessibility on a real device; pause/user-triggered retry and cross-recreation navigation remain open. |
| AgentLoop project calendar continuation | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` / `RUNTIME_VERIFICATION_PENDING` | `AGENT_LOOP_DURABLE_APPROVAL_CONTRACT.md`; a project-scoped `calendar_create` call creates a private proposal, real task/mission/run/step, and hash-only `ResumableCalendarCreate`, then pauses for Trust Center's dedicated localized private review. After one grant it atomically claims, revalidates ownership/integrity, invokes one Calendar provider insert without automatic retry, creates bounded evidence, and resumes only approved unclaimed records after bootstrap. Generic dispatcher creation and raw title/time logging are blocked. `DurableTaskProductKernelTest`, `AgentLoopExecutionContextTest`, `AgentLoopSideEffectPolicyTest`, 66/66 core checks, Kotlin compilation, and strict four-locale parity passed. | Execute Android provider permission/success/failure paths, review and denial cleanup, process recreation, duplicate-claim behavior, TalkBack, font scale, and project-switch scenarios on a physical device/emulator. Personal calendar scope and all other AgentLoop side effects remain intentionally unsupported. |
| Optional-permission onboarding | `IMPLEMENTED` / `BUILD_VERIFIED` / `RUNTIME_VERIFICATION_PENDING` | Onboarding page and permission-card text now resolve from strict four-locale resources; microphone, notification, and calendar requests stay behind a visible user button, with `PermissionsScreen` available later. Accessibility wording does not claim generic agent device control, while calendar wording states explicit proposal/review. `:app:compileDebugKotlin`, strict localization, and the dedicated 72/72 core guard passed. | Validate grant/deny/don&apos;t-ask-again and Settings return paths, RTL/LTR layout, TalkBack, font scale, and API/device permission variations on a device. It does not prove Play policy/Data safety acceptance. |
| Permission surface and Accessibility disclosure | `IMPLEMENTED` / `BUILD_VERIFIED` / `RUNTIME_VERIFICATION_PENDING` | Static review removed the unowned `SCHEDULE_EXACT_ALARM` permission and its settings card; Calendar/Contacts remain tied to explicit surfaces, while Camera requests from the chat action. Accessibility strings now state local optional enablement and policy-bound device actions across four locales. Core guard 74/74, strict localization, and `:app:compileDebugKotlin` passed. | Validate Android Settings disclosure, service activation/deactivation, grant/deny return routes, and policy behaviour on real device/API variants. This does not prove Play policy or Data safety acceptance. |
| Initial notification and referral deep-link boundary | `IMPLEMENTED` / `CI_VERIFIED` / `RUNTIME_VERIFICATION_PENDING` | `MainActivity` no longer asks for notifications at cold start; the user-controlled Onboarding button remains the request surface. Referral capture now requires a valid `airi://referral?code=` link, rejects generic/OAuth URIs, and logs no raw code. GitHub Actions run `32679669784` for `2a40a32b` passed source contracts, strict localization, debug/unit/lint/release-source, `connectedDebugAndroidTest`, and native-library verification. | Validate notification grant/deny and cold/warm referral/OAuth deep links on a device. This does not prove a real OAuth provider callback, HTTPS app-link verification, or referral/payment economics. |
| Local device-data erase | `IMPLEMENTED` / `CI_VERIFIED` / `RUNTIME_VERIFICATION_PENDING` | `DataDeletionCoordinator.eraseLocalData()` ينفذ محواً مؤكداً لبيانات AIRI المدارة محلياً فقط: يوقف WorkManager، ويمسح Room والملفات/المعرفة/artifacts والأسرار والتفضيلات وcache، ثم يوقع الخروج محلياً. لا يستدعي حذفاً بعيداً أو حذف حساب Firebase. GitHub Actions run `32690168612` للالتزام `87797631` نجح في contracts والتوطين وdebug/unit/lint/release-source و`connectedDebugAndroidTest` وفحص native output؛ كما نجحت run `32691393587` للالتزام التوثيقي `aea732d3` في البوابات ذاتها. | تحقق على جهاز حقيقي من أن الإلغاء لا يغيّر بيانات، والتأكيد يمحو كل الأسطح المحلية المتوقعة ويُبقي الحساب البعيد صالحاً. لا يشمل هذا المسار تنزيلات النماذج المحلية أو بيانات provider/cloud، ولا يثبت حذفاً بعيداً أو قبول متجر. |
| Telemetry and crash-reporting consent | `IMPLEMENTED` / `CI_VERIFIED` / `EXTERNAL_VERIFICATION_REQUIRED` | Analytics now synchronizes Firebase collection at initialization and every consent transition, without local event parameter logs. Crashlytics rejects metadata/logs/non-fatals unless explicit consent enables collection and disables immediately on revoke. GitHub Actions run `32681472224` for `0f936dd1` passed contracts, strict localization, debug/unit/lint/release-source, `connectedDebugAndroidTest`, and native-library verification. | Validate Firebase DebugView/network behaviour for opt-in, opt-out, restart, and revoke on a device; external Firebase project configuration, legal data safety, and store acceptance remain unproven. |
| Feature Freeze commercial surface boundary | `IMPLEMENTED` / `CI_VERIFIED` / `RUNTIME_VERIFICATION_PENDING` | كشف تدقيق Release Candidate أن Paywall وStripe payment وسجل الفوترة وMarketplace وCommunity Skills كانت قابلة للوصول بلا دليل provider/store. `ReleaseScopePolicy` يثبت هذه الأسطح على disabled؛ تعود routes المستعادة أو القديمة إلى شاشة مترجمة آمنة بدلاً من تهيئة Billing أو Stripe أو marketplace. أزيلت نقاط الدخول من Settings، ويزيل `tools:node="remove"` في manifest الإذن المدمج `com.android.vending.BILLING`. اختبار JVM والحارس يثبتان السياسة والـroutes والـmanifest، وGitHub Actions run `32716171238` للالتزامين `8b23ae2e` و`b1ef8b6c` اجتاز contracts والتوطين وdebug/unit/lint/release-source و`connectedDebugAndroidTest` وفحص native output. لا يُعد هذا مسار دفع حياً أو دليلاً من متجر. | يلزم merged-manifest inspection وجهاز حقيقي لمسار العودة؛ لا تعاد هذه الأسطح إلا ببرنامج تجاري منفصل مع مزود ومدفوعات وقانون/سياسة وPlay evidence. |
| Release audit baseline | `FEATURE_FREEZE` / `PARTIAL` / `CI_UNSIGNED_PACKAGE_VERIFIED` | `RELEASE_AUDIT_REGISTER.md` and `AIRI_RELEASE_CLOSURE.md` govern a release-only closure. GitHub Actions run `32720458806` for `002443a4`/`e4e36d95` passed source contracts, strict localization, shared-core tests, debug build, unit tests, lint, R8 `assembleRelease`/`bundleRelease`, APK badging, AAB ZIP verification, `mapping.txt`, `UNSIGNED_SHA256SUMS`, API 29 instrumentation, and native verification. Downloaded evidence confirms `com.airi.assistant`, versionCode `1`, minSdk `26`, targetSdk `36`, arm64-v8a, and no Billing permission. The protected main-only signing gate separately requires `mapping.txt`, `apksigner` verification/certificate output, and signed `SHA256SUMS`. | The produced APK/AAB are unsigned and not publishable. A protected `main` execution with signing secrets is required to create and verify the real signed APK/AAB, final R8 mapping, hashes, and signer evidence. Device/provider/store/legal and Data safety acceptance remain unproven. |
| Device/store verification matrix | `RUNTIME_VERIFICATION_PENDING` / `EXTERNAL_VERIFICATION_REQUIRED` | `RELEASE_DEVICE_AND_STORE_MATRIX.md` supplies traceable device/API/ABI, permission, project-isolation, calendar, scheduler, browser, native artifact, store, and provider evidence requirements. | Execute each row on the named device/CI/provider/store surface and attach commit, artifact, steps, result, and sanitized evidence; the matrix itself is not proof of release readiness. |
| Final closure program | `FEATURE_FREEZE` / `RELEASE_PUBLICATION_CLOSURE` | `AIRI_RELEASE_CLOSURE.md` controls priority; this file, the audit register, device/store matrix, and technical closure map record evidence. | Do not add roadmap features. Resolve only a demonstrated release blocker, then complete protected signing, device, provider, store, and legal evidence without labeling any external path complete before evidence exists. |

## Evidence rules

A capability may be described as `BUILD_VERIFIED` only when the relevant build/lint/package gate succeeds on the current revision. `RUNTIME_VERIFICATION_PENDING` is reserved for physical-device, authenticated-provider, multi-device, signing, or store gates that cannot be executed from the repository. It is not used to defer implementable source work.

## External verification register

| External verification | Why it is external | Prepared internal evidence |
|---|---|---|
| Android device runtime | Android permissions, picker/media behavior, TalkBack, OEM/Doze behavior | Runtime contracts, unit/policy tests, instrumentation-ready paths where present. |
| Provider/OAuth runtime | Real credentials, OAuth callbacks, external provider behavior | Sanitization, policy and connector boundary tests. |
| Continuity across devices | Paired hardware plus production sync identity/rules | Redacted snapshot merge and known-task-only safeguards. |
| Release signing/store | Private signing material and store account | Build/release manifests and deterministic reports when configured. |
