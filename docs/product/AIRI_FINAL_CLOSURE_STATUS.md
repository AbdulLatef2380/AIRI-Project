# AIRI Final Closure Status

**Branch:** `cp-foundation`  
**Program status:** `IN_PROGRESS`  
**Source of execution scope:** [`AIRI_FINAL_CLOSURE_MAP.md`](AIRI_FINAL_CLOSURE_MAP.md)

## Current milestone

| Milestone | Status | Evidence | Remaining boundary |
|---|---|---|---|
| Closure-map baseline | `IMPLEMENTED` | `AIRI_FINAL_CLOSURE_MAP.md` maps P0/P1/P2 ownership, runtime, persistence, security, UI, tests and external gates. | Map must be updated only when a real runtime or acceptance gate changes. |
| Project Context admission | `IMPLEMENTED` / `TESTED` | `ProjectContextResolver`, scoped `RagRetriever`, `ProjectContextAdmissionPolicyTest`, core verifier. | Device/model execution and broad cross-resource isolation. |
| Trust Center | `IMPLEMENTED` / `TESTED` | `TRUST_CENTER_CONTRACT.md`, live governance/durable approval bridge. | Exact-step continuation for AgentLoop, skills, terminal, and remaining connector paths; device recovery. |
| Mission ownership baseline | `IMPLEMENTED` / `TESTED` | `MissionKernel`, normalized `DurableTaskManager` persistence, `MissionKernelTest`, and `DurableTaskProductKernelTest`. | Broader cross-resource integration remains separate P0 work. |
| Project secret broker + GitHub consumer/management | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` | Project/connector-scoped capability namespace in `SecretVault`, regression coverage for cross-project denial/revocation/presence, active-workspace GitHub credential save/revoke UI that exposes no raw preview/copy, and a GitHub adapter path that validates persisted task/run/step ownership before consuming one project-bound `GITHUB_PAT` capability internally. `GitHubConnectorProjectSecretTest` injects ownership/transport and proves missing-secret or ownership mismatch never reads a legacy credential or calls the adapter, while an owned project path uses only scoped material without HTTP. | Provider adapters beyond GitHub, live GitHub/Keystore/workspace-switch verification, capability revocation during provider I/O, and accessible UI testing remain open. |
| Exact-step GitHub continuation | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` | `APPROVAL_CONTINUATION_CONTRACT.md`; durable PAUSED/CLAIMED states, restart sweep, Trust Center allow route, claimed connector authorization; `DurableTaskProductKernelTest` + `ConnectorRuntimeManagerTest` passed. | Migrate AgentLoop, skills, terminal, and remaining side-effecting connectors; validate a credentialed GitHub mutation and device process recovery. |
| Artifact execution provenance + evidence UI | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` | `ARTIFACT_PROVENANCE_CONTRACT.md`; Room v9 migration/schema, project/task/run/step/tool/model/hash metadata, project-scoped reads, orchestrator result route, and Library rendering of bounded execution evidence/integrity prefix; targeted tests passed. | Migrate every remaining producer, run instrumentation migration, and verify accessibility, preview/share/download plus multi-step provider execution on device. |
| Cross-project file/knowledge/memory/secret/artifact fixture | `BUILD_VERIFIED` | `ProjectResourceIsolationTest` compiles against real ProjectFileManager, ProjectKnowledgeManager, MemoryManager, SecretVault, and ArtifactManager paths; it asserts A/B file visibility, knowledge search, project-scoped memory recall, project-secret denial/consumption, soft-delete/restore/reindex, and artifact lookup/read denial. | Execute the instrumentation fixture on a physical device/emulator. |
| Project file lifecycle | `IMPLEMENTED` / `BUILD_VERIFIED` | `PROJECT_FILE_INTELLIGENCE_CONTRACT.md`; project-owned private trash, restore through managed storage, explicit purge, knowledge cleanup, Library recovery UI, and account filesystem wipe. | Run picker/media/delete/restore/purge sequence on a device; add rich parser and content-version coverage. |
| Memory management UI | `IMPLEMENTED` / `BUILD_VERIFIED` | `MEMORY_FABRIC_CONTRACT.md`; MemoryScreen explains scope/provenance, confirms deletion, and now edits content only for a durable long-term memory through ChatViewModel. `MemoryManager.editMemoryContent` rejects non-memory rows and preserves scope/project/privacy/importance/expiry/provenance; the projection/count refresh after success. The Room fixture compiles and four-locale parity is strict. | Run `MemoryDaoInsertIdTest.editingLongTermMemoryChangesContentAndPreservesScopedMetadata` on a device/emulator; verify delete/edit dialogs, validation failures, TalkBack, font scale, and post-restart refresh. Taxonomy expansion and user export remain open. |
| Local browser handoff boundary | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` | `BROWSER_AND_ARTIFACT_CONTRACT.md`; `LocalBrowserOperator` resolves only public browser candidates, routes HTTP(S) through `BrowserNavigationPolicy`, and emits a process-local coordinator request rather than launching an external activity. `AiriApp` displays the normalized public URL and requires a visible **Open browser** choice; the coordinator revalidates policy and calls `ACTION_VIEW` once only after that choice, handling missing/denied Android handlers safely. Coordinator, local-policy, and navigation-policy JVM tests plus core guard and strict localization passed. | Verify browser selection, cancellation, missing-handler feedback, configuration/process recreation, TalkBack, and font scale on a device. Authenticated browsing, DOM actions, upload/download flows, `geo:` app launch, and durable browser side-effect approvals remain open. |
| Project file edit proposal / approval / apply | `IMPLEMENTED` / `TESTED` / `BUILD_VERIFIED` | `PROJECT_FILE_EDITING_CONTRACT.md`; `ProjectFileEditRuntime` persists private proposals, uses a hash-only typed continuation, pauses/claims exact task/run/step, invokes only project-scoped `ProjectFileManager` revision storage, clears stale knowledge, rolls back when evidence cannot link, and sends bounded evidence to the durable timeline. Library provides user-authored text and diff review before Trust Center approval; JVM kernel/policy tests and Android fixture compilation passed with strict four-locale parity. | Run `ProjectResourceIsolationTest.textRevisionRemainsProjectOwnedAndClearsStaleKnowledge` on an emulator/device; verify approval rejection, process recreation, storage failure rollback, TalkBack, font scale, and large/read-only file behavior. |
| AgentLoop side-effect boundary | `IMPLEMENTED` / `TESTED` | `AGENT_LOOP_DURABLE_APPROVAL_CONTRACT.md`; audit found ChatViewModel's AgentLoop has no durable task/run/step context while its dispatcher can call calendar, note, alarm, accessibility, and app-intent adapters. `AgentLoopSideEffectPolicy` now blocks those calls before sandbox/adapter dispatch and retains read/conversation tools; JVM policy test and the 60/60 core guard passed. | Build a task-owned AgentLoop execution context and a typed calendar-create proposal/approval/claim runtime; test Android Calendar provider behavior. Do not treat the in-memory `ask_confirmation` sentinel as replay-safe authorization. |
| Scheduled-task evidence UI | `IMPLEMENTED` / `BUILD_VERIFIED` | `SCHEDULED_AUTOMATION_CONTRACT.md`; Agent Tasks now retains persisted completed/failed jobs instead of filtering them out, refreshes explicitly without polling, and exposes a user action to Execution Center only when `lastDurableTaskId` is present. The scheduler worker remains the producer of outcomes/task IDs; cancellation remains scoped to cancellable jobs. | Validate WorkManager timing, outcome refresh after background completion, linked-task navigation, Doze/OEM behavior, and accessibility on a real device; run-now/pause/retry and exact-task focus remain open. |
| Final closure program | `IN_PROGRESS` | This file, closure map, and `AIRI_PRODUCT_GAP_MATRIX.md`. | Continue Phase 5 with the remaining real terminal/AgentLoop continuation seams, automation recovery/UI, browser device verification, and provider/device gates; do not label any provider/device path complete before external evidence exists. |

## Evidence rules

A capability may be described as `BUILD_VERIFIED` only when the relevant build/lint/package gate succeeds on the current revision. `RUNTIME_VERIFICATION_PENDING` is reserved for physical-device, authenticated-provider, multi-device, signing, or store gates that cannot be executed from the repository. It is not used to defer implementable source work.

## External verification register

| External verification | Why it is external | Prepared internal evidence |
|---|---|---|
| Android device runtime | Android permissions, picker/media behavior, TalkBack, OEM/Doze behavior | Runtime contracts, unit/policy tests, instrumentation-ready paths where present. |
| Provider/OAuth runtime | Real credentials, OAuth callbacks, external provider behavior | Sanitization, policy and connector boundary tests. |
| Continuity across devices | Paired hardware plus production sync identity/rules | Redacted snapshot merge and known-task-only safeguards. |
| Release signing/store | Private signing material and store account | Build/release manifests and deterministic reports when configured. |
