<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release traffic accepts only the Android system trust store over TLS. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
