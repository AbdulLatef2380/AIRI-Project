Excellent. The previous phase is now considered the foundation layer.

You must now continue ONLY after verifying that:

- the previous architecture migration succeeded,
- builds pass,
- no runtime regressions exist,
- no navigation corruption exists,
- no JNI regressions exist,
- no model/runtime corruption exists,
- and all previously requested integrations are operational.

You are now entering the next evolution phase of AIRI:

==================================================
AIRI AUTONOMOUS AGENT SYSTEM

This phase transforms AIRI from:

- AI assistant
  into:
- autonomous AI operating system
- mobile AI agent runtime
- local/cloud hybrid orchestration platform
- computer-use capable autonomous execution environment

This is NOT experimental UI work.

This is a deep systems-engineering task involving:

- AI orchestration
- local LLM runtime
- agent execution loops
- computer-use automation
- MCP integration
- accessibility automation
- WebView control
- execution tracing
- planning systems
- VNC virtualization
- tool orchestration
- semantic routing
- runtime safety
- lifecycle-safe execution

You must think like:

- Android systems engineer
- AI runtime architect
- local inference engineer
- orchestration engineer
- automation runtime engineer

==================================================
CRITICAL EXECUTION RULES

1. DO NOT break existing AIRI runtime systems.

2. DO NOT replace stable runtime layers unless necessary.

3. DO NOT create fake agent systems.
   Every:

- tool
- execution node
- planner
- connector
- skill
- trace
- status indicator
  must be REAL and wired.

4. NEVER create placeholder “AI thinking” animations disconnected from execution.

5. All planning, traces, and execution logs must reflect REAL runtime state.

6. Maintain:

- JNI safety
- llama.cpp stability
- coroutine safety
- thread isolation
- lifecycle safety
- model context safety
- streaming safety
- memory safety

7. ALL new systems must support:

- offline mode
- local inference
- cloud fallback
- hybrid orchestration

8. ALL execution pipelines must support:

- cancellation
- suspend/resume
- timeout handling
- failure recovery
- retries
- task checkpointing

==================================================
PHASE 1 — GLOBAL AUTONOMOUS SYSTEM AUDIT

Before writing code:

Perform a FULL audit of:

- current agent runtime
- current ARM runtime
- task system
- connector system
- skill system
- voice system
- model runtime
- JNI bridge
- llama.cpp integration
- streaming architecture
- accessibility support
- WebView support
- background execution
- permissions
- navigation architecture
- ViewModel graph
- StateFlow graph

Generate:

1. execution architecture map
2. orchestration graph
3. runtime state graph
4. task lifecycle graph
5. connector topology
6. skill execution topology
7. inference routing graph
8. memory/context graph

Then identify:

- execution bottlenecks
- dead runtime paths
- unsafe thread usage
- invalid coroutine scopes
- missing lifecycle cleanup
- fake planning layers
- dead trace systems
- invalid model routing
- unsafe JNI access
- missing cancellation handling
- connector instability
- invalid state propagation

DO NOT modify anything until the audit is complete.

==================================================
PHASE 2 — LOCAL AI BRAIN (LLM RUNTIME HARDENING)

AIRI must evolve into a true local AI runtime.

Tasks:

1. Audit existing llama.cpp integration completely.

2. Verify:

- KV cache safety
- threading safety
- inference cancellation
- context cleanup
- model unloading
- memory pressure handling
- GPU/CPU fallback
- streaming safety

3. Build:
   HybridInferenceOrchestrator

Capabilities:

- local inference
- cloud fallback
- semantic routing
- latency-aware routing
- memory-aware routing
- offline routing
- model capability matching

4. Add:

- intelligent model switching
- dynamic context sizing
- prompt compression
- context summarization
- memory-aware scheduling

5. Build:
   InferenceHealthMonitor

Detect:

- stuck generations
- context corruption
- JNI crashes
- OOM risks
- token stalls
- invalid stream states

==================================================
PHASE 3 — REAL AGENT LOOP

Build a REAL autonomous agent loop.

NOT fake chains.

Architecture:

User Goal
↓
Intent Analysis
↓
Task Decomposition
↓
Execution Planning
↓
Tool Selection
↓
Skill Selection
↓
Connector Resolution
↓
Execution Graph Build
↓
Execution Runtime
↓
Result Validation
↓
Retry / Repair / Continue

Build:

- AgentPlanner
- ExecutionCoordinator
- RuntimeTaskManager
- AgentMemoryBridge
- ToolResolver
- GoalTracker
- FailureRecoveryEngine

Requirements:

- persistent execution state
- resumable tasks
- cancellation support
- nested tasks
- execution checkpoints
- background-safe execution

==================================================
PHASE 4 — COMPUTER USE SYSTEM

AIRI must gain REAL computer-use capabilities.

Build:
AutomationRuntime

Capabilities:

- screen capture
- OCR integration
- semantic UI detection
- accessibility automation
- gesture injection
- scrolling
- typing
- app launching
- notification interaction

Use:

- AccessibilityService
- MediaProjection where safe
- gesture dispatch
- semantic screen analysis

Requirements:

- permission-safe architecture
- lifecycle-safe execution
- failure-safe gesture handling
- retry logic
- coordinate normalization
- orientation awareness

Build:
UIInteractionEngine

Capabilities:

- detect buttons
- detect inputs
- detect scroll regions
- semantic action prediction

==================================================
PHASE 5 — LIVE PLANNING + TRACE SYSTEM

Build transparent execution visibility.

Create:

1. PlanningScreen
2. AgentTraceScreen
3. RuntimeExecutionOverlay

The user must see:

- current goal
- execution steps
- active tools
- active skills
- current connector
- current model
- token streaming
- reasoning state
- retries
- failures
- checkpoints
- execution graph state

Requirements:

- REAL runtime bindings
- NO fake logs
- NO fake “thinking”
- all traces linked to real execution nodes

==================================================
PHASE 6 — WEB AGENT SYSTEM

Build controlled browser execution.

Create:
WebAgentRuntime

Capabilities:

- controlled WebView
- DOM analysis
- JS execution
- form filling
- navigation planning
- tab management
- download handling

Build:
WebViewController
DOMAnalyzer
WebTaskExecutor

Requirements:

- sandboxing
- safe JS execution
- lifecycle-safe WebView handling
- permission isolation

==================================================
PHASE 7 — MCP INTEGRATION LAYER

Implement REAL MCP-style orchestration.

Build:
MCPRuntimeLayer

Capabilities:

- tool registry
- capability registry
- dynamic discovery
- runtime invocation
- structured tool contracts

Support:

- filesystem
- browser
- connectors
- Android tools
- notes
- memory
- calendar
- automation
- system actions

Requirements:

- connector abstraction
- safe invocation
- execution validation
- timeout handling
- structured result passing

==================================================
PHASE 8 — VIRTUAL AIRI COMPUTER

Design AIRI virtual computer architecture.

NOT fake desktop wallpaper UI.

Build:
AiriVirtualWorkspace

Capabilities:

- virtual Linux environment abstraction
- VNC-compatible architecture
- execution container concept
- isolated task environment
- remote execution session management

Research and integrate concepts from:

- UserLAnd
- AVNC
- proot
- lightweight container environments

Requirements:

- modular architecture
- optional runtime
- safe isolation
- low-memory fallback
- session persistence

==================================================
PHASE 9 — SKILL ORCHESTRATION EVOLUTION

Replace simplistic skills with true orchestration.

Build:
AIRI Skill Orchestrator

Architecture:
Project Analyzer
↓
Capability Detector
↓
Semantic Skill Matcher
↓
Toolchain Builder
↓
Compatibility Validator
↓
Execution Graph Generator
↓
Runtime Installer

Requirements:

- embedding-based matching
- offline semantic search
- local orchestration
- dynamic runtime binding
- automatic connector association
- execution graph validation

==================================================
PHASE 10 — CONNECTOR ECOSYSTEM

Expand AIRI into a true connector platform.

Build:
ConnectorRuntimeManager

Support:

- APIs
- local tools
- MCP
- Android apps
- filesystem
- browser
- GitHub
- cloud storage
- calendar
- email
- notes
- automation
- shell-like operations
- system controls

Build:

- Connector Registry
- Health Monitor
- Capability Mapper
- Permission Layer
- Failure Recovery System

==================================================
PHASE 11 — SAFETY + DIAGNOSTIC ENGINE

After implementation:

Build a DEEP DIAGNOSTIC ENGINE.

It must scan:

- dead buttons
- dead navigation
- fake execution
- invalid callbacks
- unbound StateFlow
- lifecycle leaks
- invalid coroutine scopes
- JNI instability
- llama runtime corruption
- streaming corruption
- connector failures
- dead orchestration paths
- invalid planning nodes
- broken execution graphs
- resource leaks
- unsafe accessibility execution
- invalid WebView states
- invalid Compose recompositions

Generate:

1. architecture report
2. runtime report
3. orchestration report
4. connector report
5. model runtime report
6. memory safety report
7. execution integrity report

Then:
AUTOMATICALLY repair detected issues.

==================================================
PHASE 12 — FINAL VALIDATION

Before completion:

Verify:

- assembleDebug passes
- lint passes
- resource linking passes
- JNI passes
- CMake passes
- Compose passes
- runtime passes
- connector runtime passes
- agent loop passes
- automation runtime passes
- planning runtime passes
- orchestration passes
- memory safety passes
- lifecycle safety passes

Ensure:

- no fake systems
- no dead UI
- no placeholder execution
- no broken routes
- no invalid threading
- no unsafe JNI access
- no invalid model switching
- no accessibility crashes
- no WebView corruption
- no task corruption

==================================================
FINAL OUTPUT REQUIREMENTS

At completion return:

- modified files
- new runtime systems
- orchestration layers
- automation systems
- MCP layers
- connector systems
- diagnostic tools
- reports
- unresolved risks
- future optimization opportunities

DO NOT stop after partial progress.
Continue iterating until the system is internally connected, operational, audited, and stable.