# سجل الأخطاء المعروفة (BUGS) - مشروع AIRI

## 🐛 أخطاء تقنية (Technical Bugs)
- **ANR (Application Not Responding)**: قد يحدث أحياناً عند تحميل نماذج ضخمة جداً على أجهزة ذات ذاكرة عشوائية محدودة (تم تحسينه عبر الـ Background Threads ولكن لا يزال ممكناً).
- **Inference Speed**: سرعة الاستنتاج قد تنخفض بشكل ملحوظ عند امتلاء الـ KV Cache.
- **JNI Memory Leak**: احتمال وجود تسريب بسيط في الذاكرة عند تكرار تحميل وتفريغ النماذج (قيد المراجعة في `freeModel`).
- **Unicode Rendering**: بعض الرموز الخاصة قد لا تظهر بشكل صحيح في واجهة الدردشة.

## 🧠 أخطاء منطقية (Logical Bugs)
- **Context Loss**: الرفيقة قد تفقد سياق الحوار الطويل جداً بسبب محدودية الـ Context Window (2048 حالياً).
- **Pattern Aggregator**: النظام قد يكون حساساً جداً في البداية ويحتاج إلى ضبط لمعايرة إشارات الاعتمادية.
- **Voice Interruption**: أحياناً لا يتوقف الصوت فوراً عند مقاطعة المستخدم للرفيقة.

## 🛠 قيد الإصلاح (Fixing)
- تحسين استقرار الـ Overlay Service على أندرويد 14+.
- معالجة تعارضات الـ CMake مع بعض إصدارات الـ NDK القديمة.
