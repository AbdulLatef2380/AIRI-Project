You are now entering a full production-grade architecture recovery and enhancement phase for the AIRI Android AI system.

This is NOT a UI-only task.
This is a deep runtime, orchestration, model-management, connectors, skills, and system-integration operation.

You must operate as a senior Android AI systems engineer.

Your mission:
Perform a COMPLETE architecture audit, dependency audit, runtime audit, JNI audit, llama.cpp audit, Compose audit, navigation audit, connector audit, skill orchestration audit, and UI interaction audit BEFORE modifying code.

You must:

- analyze
- plan
- divide phases
- execute
- validate
- re-audit
- repair
- repeat

until the project becomes fully integrated, production-safe, and internally connected.

DO NOT stop after partial completion.

==================================================
GLOBAL EXECUTION RULES

1. NEVER remove existing working runtime logic unless explicitly replaced safely.

2. NEVER break:

- JNI
- llama.cpp
- ViewModels
- navigation
- StateFlow pipelines
- memory systems
- ARM runtime
- voice runtime
- connectors
- streaming
- model loading
- local inference

3. NEVER create fake UI.
   Every button, toggle, switch, card, screen, and interaction MUST be fully wired to real runtime logic.

4. NEVER leave placeholder systems.

5. NEVER create dead screens or dead navigation routes.

6. NEVER hardcode invalid states.

7. ALWAYS preserve backward compatibility.

8. ALWAYS verify:

- imports
- dependencies
- Gradle
- Compose BOM
- Material compatibility
- theme compatibility
- StateFlow wiring
- JNI layer
- CMake
- native bridges
- lifecycle safety
- coroutine threading
- memory safety
- model unloading
- streaming safety

9. AFTER EVERY MAJOR PHASE:
   run a full cross-reference audit.

10. DO NOT stop until:

- build passes
- resources pass
- runtime passes
- UI passes
- connectors pass
- model systems pass
- skills pass
- audits pass

==================================================
PHASE 1 — FULL SYSTEM AUDIT

Start by reading and auditing:

- app/src/main/java/
- app/src/main/cpp/
- app/src/main/res/
- navigation
- ViewModels
- JNI bridge
- llama.cpp integration
- STT/TTS
- ARM runtime
- connectors
- memory system
- skills
- task runtime
- model management
- Gradle
- libs.versions.toml
- manifests
- themes
- all Compose UI layers

Then generate:

1. architecture map
2. runtime map
3. dependency graph
4. navigation graph
5. model loading flow
6. skill execution flow
7. connector flow
8. JNI bridge flow
9. streaming flow
10. voice pipeline flow

Then identify:

- broken layers
- dead code
- fake UI
- missing runtime wiring
- placeholder systems
- unbounded layers
- unsafe JNI calls
- coroutine races
- invalid model state handling
- missing lifecycle cleanup
- invalid navigation states
- duplicated dependencies
- broken imports
- dead connectors
- dead skills
- dead screens
- invalid Compose state
- invalid threading
- unsafe streaming
- memory leaks

DO NOT modify anything yet until the full audit completes.

==================================================
PHASE 2 — LOCAL + CLOUD MODEL SYSTEM RESTORATION

The local model marketplace/store was removed.
Restore it completely from the old app project.

I will provide:

- old app folder
- current app folder

Tasks:

1. Restore the local model store exactly as before.

2. Add a SECOND section:
   Cloud Models

3. Add:

- local model selection
- cloud model selection
- active model switching
- smart switching engine

4. Build:
   Hybrid Model Orchestrator

Capabilities:

- automatic local/cloud switching
- fallback handling
- context-aware routing
- latency-aware routing
- offline-aware routing
- memory-aware routing

5. Ensure:

- model state persists
- switching is safe
- unloading/loading is safe
- no JNI crashes occur
- no llama context corruption occurs

6. Ensure:

- model manager
- inference runtime
- chat runtime
- streaming runtime
- embeddings runtime
  all support model switching correctly.

==================================================
PHASE 3 — CHAT LOCK + MODEL SAFETY

The user MUST NOT be able to start chatting unless:

- a local model is installed
  OR
- a cloud model is active

If no model exists:

Disable:

- input bar
- send button
- voice input
- live chat
- streaming

Show proper state UI:

- install model
- choose cloud model
- loading state
- unavailable state

All checks must be runtime-safe and lifecycle-safe.

==================================================
PHASE 4 — INPUT BAR + ACTION MENU RESTRUCTURE

The bottom action row under the input bar is overcrowded.

Tasks:

1. Redesign interaction flow.

2. Either:

- auto-hide controls contextually
  OR
- move them into expandable action menu

3. Build:
   Smart Action Surface

Features:

- adaptive controls
- gesture-safe
- compact mode
- voice mode
- streaming mode
- keyboard-aware layout
- landscape support

4. Ensure:

- no overlapping UI
- no hidden actions
- no dead buttons
- all actions wired

==================================================
PHASE 5 — LANGUAGE SYSTEM RESTORATION

Language selection was removed.

Restore:

- Arabic
- English
- multilingual support

Tasks:

1. Restore language settings screen.

2. Add:

- runtime language switching
- locale persistence
- UI recreation safety
- multilingual resources

3. Ensure:

- Compose recomposes correctly
- no resource crashes
- RTL/LTR works perfectly
- mixed-language chat remains supported

==================================================
PHASE 6 — CLOUD MODEL REWORK

Current cloud model API-key workflow is unacceptable.

Remove:

- manual API entry by user

Build:
Integrated Cloud Runtime

Requirements:

1. Embed provider routing internally.

2. Use FREE high-quality models/providers.

3. Configure APIs internally.

4. Add:

- provider abstraction layer
- provider health checks
- fallback providers
- retry logic
- rate limit handling
- timeout handling

5. Ensure:

- secure key handling
- encrypted storage
- no plaintext exposure
- offline fallback behavior

6. Verify all providers actually work.

==================================================
PHASE 7 — AIRI SKILL ORCHESTRATOR

The current manual skill configuration UX is obsolete.

Replace the current approach with:

AIRI Skill Orchestrator

Architecture:

Project Analyzer
↓
Capability Detector
↓
Skill Recommendation Engine
↓
Compatibility Validator
↓
Runtime Installer
↓
Execution Graph Builder

Requirements:

1. Detect available capabilities automatically.

2. Build semantic skill matching.

3. Build embeddings for:

- skills
- tasks
- connectors
- intents

4. Support:

- local semantic search
- offline orchestration
- dynamic toolchains
- automatic runtime binding

5. Build:
   Execution Planner

6. Build:
   Toolchain Generator

7. Build:
   Offline-capable orchestration

8. Build:
   dynamic execution graph runtime

9. Ensure:

- no fake skills
- no dead skill cards
- no placeholder execution
- all skills executable

==================================================
PHASE 8 — CONNECTOR EXPANSION

The connector system must become production-grade.

Tasks:

1. Add MANY real connectors.

2. Support:

- APIs
- local tools
- MCP
- apps
- system connectors
- web tools
- filesystem
- GitHub
- browser
- Android system
- automation
- notes
- calendar
- email
- cloud storage

3. Build:
   Connector Registry

4. Build:
   Connector Health Monitor

5. Build:
   Permission Safety Layer

6. Build:
   Connector Runtime Manager

7. Ensure:

- connector persistence
- runtime binding
- execution validation
- safe threading
- failure recovery

==================================================
PHASE 9 — FULL INTEGRATION PASS

After all phases:

Perform a COMPLETE integration audit.

Verify:

- every button
- every toggle
- every screen
- every route
- every connector
- every skill
- every model state
- every ViewModel
- every StateFlow
- every JNI bridge
- every llama context
- every coroutine
- every streaming pipeline
- every memory layer
- every Compose state
- every navigation transition

==================================================
PHASE 10 — DEEP DIAGNOSTIC ENGINE

After implementation:

Create a FULL internal diagnostic system.

Build scripts/tools that scan for:

- dead UI
- fake buttons
- missing callbacks
- unbound StateFlow
- invalid navigation
- JNI crashes
- llama runtime issues
- memory corruption
- coroutine races
- threading issues
- connector failures
- model switching failures
- invalid imports
- duplicate dependencies
- Gradle problems
- Compose recomposition issues
- lifecycle leaks
- unsafe context access
- invalid ViewModel scope usage
- resource leaks
- broken themes
- invalid Material setup
- dead execution graphs
- broken orchestration paths

Then:

Generate:

1. diagnostic report
2. repair plan
3. execution plan

Then AUTOMATICALLY execute repairs.

==================================================
PHASE 11 — FINAL VALIDATION

You MUST finish with:

1. Full compile validation
2. Full Gradle validation
3. Full resource validation
4. Full Compose validation
5. Full JNI validation
6. Full connector validation
7. Full orchestration validation
8. Full runtime validation
9. Full navigation validation
10. Full memory/runtime safety validation

Then verify:

- no crashes
- no dead UI
- no fake systems
- no broken themes
- no unresolved imports
- no missing resources
- no invalid threading
- no runtime corruption

==================================================
FINAL OUTPUT REQUIREMENTS

After completion:

1. Return:

- modified files
- newly added files
- diagnostic scripts
- orchestration layers
- connector layers
- runtime reports

2. Include:

- architecture summary
- runtime summary
- integration summary
- unresolved risks
- future optimization opportunities

3. Ensure the project builds successfully with:
   "./gradlew assembleDebug"

4. Ensure GitHub Actions CI passes fully.

5. DO NOT stop early.

6. DO NOT simplify the task.

7. DO NOT replace systems with placeholders.

8. DO NOT remove features unless explicitly requested.

9. DO NOT leave partial integrations.

10. Continue iterating until the system is fully operational and internally connected.