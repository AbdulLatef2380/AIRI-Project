# AIRI — تدقيق جذور المشكلات 01–27B

> **نوع الوثيقة:** خط أساس تدقيق قبل دفعة إصلاح جديدة. لا تعتبر هذه الوثيقة أي feature مكتملة لمجرد وجود route أو class أو نجاح CI. الحالات المستخدمة هي الحالات المعيارية في المرفق، مع فصل صريح بين `SOURCE_VERIFIED` و`CI_VERIFIED` و`RUNTIME_PENDING` و`EXTERNAL_PENDING`.
>
> **نقطة التدقيق:** فرع `cp-foundation`، والرأس الحالي `b4ea839788d04b25d8ea1c7648ccddf15f2acf7b`، المتطابق حالياً مع `main`. يحتوي المصدر على 658 ملف Kotlin و66 اختبار JVM و55 شاشة، وقد فُحصت بنية التطبيق وManifest ومواضع الحالة والتخزين والشبكة ودورة الحياة. الرأس المرجعي الذي ذكره المرفق (`8656fd12`) ليس نقطة الرأس الحالية؛ لذلك استُخدمت فروق Git والأدلة الحالية بدلاً من نسب نتائج جديدة إلى ذلك الرأس.

## خلاصة جذور مشتركة أولية

| المعرّف | الجذر المشترك المرشح | الأعراض المتأثرة | درجة الإثبات الحالية |
|---|---|---|---|
| RC-01 | تشتت ملكية الحالة بين `ChatViewModel` و`SharedPreferences` وRoom ومديري runtime متعددين | 03، 07، 08، 11، 13، 19، 27B | `ROOT_CAUSE_FOUND` جزئياً؛ يلزم تتبع كل مسار كتابة/قراءة قبل إعادة التصميم. |
| RC-02 | طبقات كثيرة تعتمد على موفّرات مباشرة أو خدمات منفردة بدلاً من عقد capability موحدة | 02، 07، 08، 17، 18، 22، 23، 24 | `ROOT_CAUSE_FOUND` جزئياً من الجرد؛ اكتمال الربط لكل موفر `INVESTIGATING`. |
| RC-03 | حدود lifecycle/process recreation وملكية coroutine غير موحدة | 07، 11، 18، 23، 27B | `INVESTIGATING`؛ توجد سياسات وحراس ناجحة، لكن لا يوجد دليل جهاز شامل. |
| RC-04 | مسارات المنتج تعرض قدرات قبل وجود مصدر حقيقي أو اعتماد أو تحقق runtime | 06، 17، 18، 21، 22، 23، 25، 26 | `ROOT_CAUSE_FOUND` في الأسطح المدققة؛ بقية الشاشات تحتاج مسحاً سطرياً. |
| RC-05 | فجوة بين حالة المصدر/CI وحالة الجهاز/الموفر/المتجر | جميع المسائل ذات البوابة الخارجية | `VERIFIED` كقيد منهجي؛ لا يمكن تحويله إلى runtime pass دون الموارد الخارجية. |

## مصفوفة التدقيق

| ID | المشكلة | Root Cause | Evidence | Affected Layers | Severity | Current State | Fix Plan | Tests | Runtime Gate |
|---|---|---|---|---|---|---|---|---|---|
| 01 | Arabic-first localization | موارد اللغة موجودة، لكن خطر النصوص الصلبة وتفاوت الشاشات لا يُحسم بالمسح وحده؛ يلزم ربط كل نص UI بالموارد ومراجعة bidi/format. | `scripts/airi_localization_health.py` والحارس نجحا في الدفعات الأخيرة؛ وجود 4 موارد لغة مثبت بالجرد. | UI، resources، accessibility، formatting | P0 | `CI_VERIFIED` / `RUNTIME_PENDING` | فهرسة النصوص الصلبة، توحيد plural/date/number، ثم مراجعة RTL وfont scale. | localization health، strict parity، UI matrix | جهاز عربي/إنجليزي/إسباني/صيني مع RTL/LTR وfont scale. |
| 02 | Authentication | طبقة Firebase وOAuth متعددة المسارات؛ النتيجة تعتمد على callback/provider/session، وبعضها لا يملك تحققاً حياً في هذه الجلسة. | `AuthService.kt`، `GoogleAuthService.kt`، `OAuthStateRegistry.kt`، إصلاح `780e9a44`، وManifest callback. | UI، auth، OAuth، secure storage، session | P0 | Google `CI_VERIFIED` / `RUNTIME_PENDING`; GitHub/provider `EXTERNAL_PENDING` | توحيد sealed outcomes، callback/state/expiry/revoke/account switching، ومنع secret داخل APK. | policy tests، auth unit/integration حيث يمكن | حسابات اختبار Google/GitHub، جهاز ARM64، callback وnetwork/provider failures. |
| 03 | Points/Credits | كان عداد التفاعل وtoken accounting مصدرين مختلفين؛ عولج الفصل لكن يلزم تدقيق event lifecycle لكل request/retry/cancel. | `CreditMeteringEngine.kt`، `TokenAccountant`، commit `1aed53e9`. | account، request، model، response، persistence | P0 | `CI_VERIFIED` / lifecycle audit pending | بناء event ledger idempotent يربط requestId بالحالة والتكلفة، وعدم الخصم قبل inference. | policy/unit، failure/cancel/retry tests | قياس provider/local failures والـpartial streaming على جهاز/موفر. |
| 04 | Chat overflow قبل المحادثة | عمليات menu لم تكن مربوطة بحالة session ذات محتوى/عنوان صالح. | `ChatSessionActionPolicy.kt`، commit `584956c2`، وCI السابق. | Chat UI، session state، persistence | P1 | `CI_VERIFIED` / `RUNTIME_PENDING` | إبقاء eligibility مركزية وإضافة UI navigation/empty/error coverage. | policy/unit، Compose UI لاحقاً | جهاز: no conversation/empty/active/archived وكل actions. |
| 05 | Home welcome | greeting كان يعتمد على نص ثابت/هوية غير موحدة بدلاً من Account→Profile→DisplayName. | commitات `c705f3e6` و`53c1fde4` و`0a99582b`، empty-state localization. | auth، profile، home UI، resources | P1 | `CI_VERIFIED` / `RUNTIME_PENDING` | تدقيق profile fallback، account switch، empty display name، والـavatar. | profile/unit، localization | جهاز مع مزود هوية وحسابين وlogout/login. |
| 06 | الملاحظات والبطاقات المزعجة | فصل debug/internal عن production لم يكن شاملاً؛ بعض notices/status cards قد تمثل حالة أكثر من قيمة للمستخدم. | إصلاح `9771d227` للأسطح الداخلية؛ جرد Settings/screens؛ يلزم مسح البطاقات المتبقية. | UI، release policy، resources | P1 | `ROOT_CAUSE_FOUND` جزئياً / `CI_VERIFIED` للأسطح الداخلية | جرد snackbar/banner/tooltip، وتصنيف user-facing مقابل internal، ثم حذف الضوضاء لا السلوك. | source contracts، Compose UI review | جهاز release مع كل model/conversation flows. |
| 07 | Models لا تستجيب | pipeline طويل متعدد المالكين؛ احتمال فقد state/connector/streaming/cancellation لا يثبت من وجود router. | `RuntimeRouter.kt`، `AgentRouter.kt`، `ChatViewModel.kt`، `RemoteModelSelectionPolicy.kt`؛ لا provider credentials حية. | UI، ViewModel، router، connector، persistence | P0 | `INVESTIGATING` / previous targeted CI only | trace requestId من send إلى persistence، اختبارات second/10th/long/error/timeout/cancel، ثم provider matrix. | unit policies، integration fake contract only، instrumentation | local/cloud models حقيقية، network/provider failures، process recreation. |
| 08 | اختيار model لا يُحترم | توجد سياسات حديثة لمنع callback القديم، لكن يجب إثبات binding بين persisted selection وconversation وrouter لكل provider. | `ModelLoadRequestPolicy.kt`، `RemoteModelSelectionPolicy.kt`، commit `1e1dda40`. | model state، conversation، router، persistence | P0 | `CI_VERIFIED` جزئياً / `RUNTIME_PENDING` | مصدر حقيقة واحد للـmodel binding مع explicit fallback policy وعدم random selection. | policy/unit، multi-session tests | تبديل نموذج/جلسة أثناء loading وعلى offline/online. |
| 09 | Planning panel | الخطر الجذري هو عرض خطوات غير منشأة أو thinking غير حقيقي؛ عولج tracker ليعرض actions admitted فقط. | `TaskExecutionTracker.kt`، `ExecutionStatusBus.kt`، commits `ed0eda58` و`fe7b4933`. | agent، status bus، plan UI، persistence | P0 | `CI_VERIFIED` / `RUNTIME_PENDING` | تدقيق event ordering، executionId، cancel/fail/restart، ثم UI semantics دون chain-of-thought. | tracker/unit، agent policy، instrumentation | جهاز مع tool action حقيقي وfailure/cancel/process kill. |
| 10 | Attachment UI | واجهة المرفق تعتمد على صحة pipeline؛ الحالة المرئية لا تكفي إن لم يُحسم preview/progress/retry/cancel. | `AttachmentDispatchPolicy.kt`، `ChatComposerDraft.kt`، commit `8de00ea2`؛ فحص أمن القراءة موجود. | picker، composer، processing، model context، UI | P0 | `CI_VERIFIED` جزئياً / `RUNTIME_PENDING` | state machine موحدة للـqueued/processing/ready/failed/cancelled مع preview آمن. | policy/unit، attachment security scan | camera/files/audio/document permissions، large/unsupported files، cancel. |
| 11 | فقدان input state | كانت المسودة transient وغير معزولة دائماً حسب session/model؛ عولج النص والمرفقات وسباق تبديل الجلسة. | `ChatComposerDraft.kt`، commit `e9138975`، tests/CI. | Compose، ViewModel، session، persistence/restoration | P0 | `CI_VERIFIED` / `RUNTIME_PENDING` | تدقيق voice/live/lifecycle/process recreation وownership عند account switch. | draft policy، instrumentation/UI | rotation/background/kill/restart/model/session/voice. |
| 12 | Attachment ingestion | الخطر هو فقدان العلاقة بين URI وpermission وmetadata وprocessed context، لا filename فقط. | `AttachmentDispatchPolicy.kt`، source security guard وattachment regression tests؛ يلزم runtime evidence. | picker، content resolver، storage، context، persistence | P0 | `CI_VERIFIED` / `RUNTIME_PENDING` | typed ingestion result مع bounded read، ownership، dedupe، failure/cancel، context admission. | security scan 3/3، JVM policy، integration/device | revoked URI، unreadable file، traversal/large file، provider/model rejection. |
| 13 | Conversations/History | وجود Room/session APIs لا يثبت اكتمال UX؛ عمليات البحث/الترتيب/الأرشفة/التحديد والتزامن تحتاج مالكاً موحداً. | `ChatViewModel.kt` وsession policies؛ إصلاح delete failure في `a79f45a5`. | Room، repository، history UI، sharing/export | P1 | `CI_VERIFIED` جزئياً / `RUNTIME_PENDING` | state machine للحمل/الخطأ/الصفحات والعمليات، مع tests ownership وmulti-select. | policy/unit، Room/instrumentation | جهاز مع restart/delete failure/archive/export/share/accessibility. |
| 14 | Expandable Input | input يحتاج state قابل للتوسع لا مجرد field؛ قد يكون السلوك موجوداً جزئياً ويجب إثبات cursor/selection والـattachments. | `ChatScreen.kt` و`ChatComposerDraft.kt`؛ لا يوجد دليل UI device في الجلسة. | Compose، draft، attachments، keyboard | P1 | `INVESTIGATING` | مراجعة layout/scroll/selection وIME وfont scale قبل تعديل تجميلي. | Compose UI، draft unit | جهاز عربي/إنجليزي، نص طويل، keyboard/rotation/selection. |
| 15 | Language mixing | parity checker يمنع مفاتيح ناقصة لكنه لا يكشف كل hardcoded provider/errors أو bidi المرئي. | `airi_localization_health.py` و4 strings resources؛ نتائج strict السابقة. | all UI/resources/errors/accessibility | P0 | `CI_VERIFIED` / `RUNTIME_PENDING` | استخراج strings صلبة وتصنيف technical terms، مراجعة format/plurals/bidi. | localization contracts، screenshot/UI tests | اللغات الأربع مع RTL/LTR وTalkBack. |
| 16 | Theme/Modes | scattered colors أو حالات theme غير semantic قد تنتج contrast/layout corruption رغم نجاح compile. | ثيم Compose ومواضع colors تحتاج probe كامل؛ CI لا يثبت contrast. | theme، UI، typography، dynamic color | P1 | `INVESTIGATING` | inventory للألوان، semantic tokens، contrast checks، light/dark/system/dynamic snapshots. | source scan، Compose screenshot/UI | أجهزة light/dark/system/dynamic وfont scale. |
| 17 | Integrations screen | route/icon لا يثبت capability/auth/consent/execution/revoke؛ بعض التكاملات source-only. | `IntegrationsViewModel.kt`، `GoogleConnector.kt`، `ConnectorsViewModel.kt`، `780e9a44` و`b19b9fb2`. | UI، connector registry، auth، secrets، network | P0 | Google `CI_VERIFIED`; Zapier/IFTTT disabled و`CI_VERIFIED`; others `EXTERNAL_PENDING` | capability state موحد: not configured/authorized/failed/revoked، ومنع claims ثابتة. | connector/policy/unit | حسابات اختبار وموافقة/إلغاء/revoke لكل موفر معلن. |
| 18 | Audio + Wake Word | pipeline يجمع microphone/VAD/STT/wake/TTS/lifecycle وخدمات foreground؛ status لا يكفي لإثبات readiness. | `VoiceManager.kt`، `LiveVoiceService.kt`، `VoskModelManager.kt`، `VoiceCapabilityPolicy.kt`، `84121afa`. | permission، audio service، VAD/STT/TTS، lifecycle | P0 | `CI_VERIFIED` / `RUNTIME_PENDING` | state machine للصوت مع resource release وforeground/background وfalse-positive cooldown. | `VoiceCapabilityPolicyTest`، instrumentation/device matrix | ARM64 API 26/35/36 مع deny/revoke/background/model unavailable. |
| 19 | Credits/Usage consumption | event boundary بين created/accepted/inference/complete/fail/cancel قد يختلف عن token accountant. | `CreditMeteringEngine.kt`، `TokenAccountant`، `1aed53e9`. | request، model، billing-like local meter، persistence | P0 | `CI_VERIFIED` جزئياً / `RUNTIME_PENDING` | idempotency وrollback/no-charge rules، وربط local/cloud costs بعقد واحد. | unit/policy، failure/retry/cancel | provider/local execution حقيقي مع timeout/partial/cancel. |
| 20 | Permissions | الشاشات سابقاً خلطت optional/special/runtime permissions؛ عولجت notification/storage/accessibility display. | `PermissionDisplayPolicy.kt`، `AccessibilityServiceState.kt`، `5a5bf3bb` و`e1cc6604`. | Manifest، Onboarding، settings، services | P0 | `CI_VERIFIED` / `RUNTIME_PENDING` | مراجعة كل permission why/when/where/grant/deny/settings-return/state refresh. | policy/unit، CI instrumentation | API 26 و35/36 grant/deny/permanent deny/revoke/Settings. |
| 21 | Updates | شاشة التحديث كانت تعرض حالة release/notes/install مصطنعة بلا catalog موثوق. | `UpdateAvailabilityPolicy.kt`، `a648c47a`، `UpdateScreen.kt`. | release metadata، UI، download/install | P1 | `CI_VERIFIED` / `EXTERNAL_PENDING` | إبقاء Not Configured حتى وجود source حقيقي؛ عند إضافته عقد verify/download/handoff. | policy/unit، source contract | متجر/قناة release مصرح بها أو تحقق خارجي؛ لا fake progress. |
| 22 | Zapier/IFTTT | OAuth/webhook/credential paths كانت قابلة للعرض والتنفيذ قبل وجود provider evidence؛ عولجت fail-closed. | `ZapierConnector.kt`، `IftttConnector.kt`، `ReleaseScopePolicy.kt`، `b19b9fb2`. | settings، route، connector، secure storage، network | P0 | `CI_VERIFIED` / intentionally disabled | الاحتفاظ بعقد capability قابل للتوسعة، وعدم تفعيلها دون credentials/consent/revoke. | policy/source contracts، CI | owner-provided sandbox credentials and provider approval only. |
| 23 | Terminal/Workspace/Sandbox | وجود UI/manager لا يثبت process execution/isolation/persistence؛ release gate يحجب بعض الأسطح الداخلية. | `Terminal`/`SandboxManager`/workspace sources، `9771d227`؛ لا Android runtime. | UI، process، filesystem، approvals، security | P0 | `SOURCE_VERIFIED` جزئياً / `RUNTIME_PENDING` | فصل internal tooling عن release، ثم إثبات sandbox isolation/lifecycle قبل أي user surface. | static/security/policy | جهاز/بيئة تنفيذ مصرح بها، process kill/cleanup/path traversal. |
| 24 | Secrets | `SecureStorage` وCrash/Firebase wrappers موجودة، لكن كل read/write/log/rotation/provider path يجب أن يثبت عدم التسريب. | `SecureStorage.kt`، `FirebaseCrashReporter.kt`، security scan؛ failure modes تحتاج audit. | UI، vault، connectors، logs/crash/analytics | P0 | `SOURCE_VERIFIED` / `EXTERNAL_PENDING` | fail-closed encrypted storage، redaction boundary، account switch/delete/rotation tests. | `SecureApiKeyStoreTest`، security scan | device keystore, crash/analytics consent and provider request inspection. |
| 25 | Security Scanner | scanner قد يمنح confidence زائفة إذا أثبت وجود function فقط بدلاً من السلوك/الحد. | `tools/security_scan.py` وsource contracts؛ يلزم تدقيق coverage/negative cases. | scanner، release config، manifest، network/files/shell | P0 | `SOURCE_VERIFIED` / `INVESTIGATING` | ربط كل finding بعقد سلبي/إيجابي، منع pass إذا لم يُختبر path، وإخراج evidence provenance. | security scan 8/8 وattachment 3/3 سابقاً؛ توسيع regression | artifact/manifest/runtime exploit-oriented review مصرح. |
| 26 | About AIRI | About قد يحتوي version/build لكن اكتمال license/acknowledgements/privacy/links/security contact يحتاج مصدر موثق وترجمة. | `AboutScreen.kt`/resources تحتاج audit مباشر؛ لا يُستنتج اكتمالها من CI. | UI، build metadata، legal/privacy/resources | P1 | `INVESTIGATING` | جرد الحقول ومصادرها، وإظهار روابط موجودة فقط بصياغة مترجمة. | resource/source tests، UI | جهاز/روابط فعلية؛ legal owner review للسياسة والشروط. |
| 27A | User Profile/Account | lifecycle موزع بين Firebase user و`UserProfileRepository` وpreferences/session؛ يلزم تدقيق ownership وremote/local distinction. | `SessionManager.kt`، `UserProfileRepository.kt`، `AuthService.kt`، إصلاحات greeting/delete السابقة. | auth، profile، preferences، local/remote data | P0 | `CI_VERIFIED` جزئياً / `RUNTIME_PENDING` | عقد account identity واحد، profile fallback، switch/logout/delete/reauth، transactions. | profile/unit، deletion/policy tests | حسابان، logout/login، switch، delete/reauth، process recreation. |
| 27B | Model Response/Reasoning/Execution UX | event stream بين send/router/agent/stream/persistence/UI يحتاج ownership واضح؛ يجب عرض summary آمن لا chain-of-thought. | `ExecutionStatusBus.kt`، `AgentLoop.kt`، `TaskExecutionTracker.kt`، `ChatViewModel.kt`. | chat، agent، router، streaming، persistence | P0 | `CI_VERIFIED` جزئياً / `RUNTIME_PENDING` | typed lifecycle للأحداث، requestId/executionId، cancellation، partial/error/retry، وsummary مترجم. | tracker/policy/unit، instrumentation | first/second/10th request، long/timeout/cancel/process recreation/provider failure. |

## ملاحظة حالة التدقيق

النتائج الحالية لا تعني أن المسائل ذات `CI_VERIFIED` اجتازت تجربة المستخدم أو جهازاً حقيقياً. لا يوجد في هذه الجلسة Android SDK/adb/emulator/ARM64 device أو device farm مصرح، ولا credentials اختبارية لمزودي AIRI، ولا جلسة Play Console أو اعتماد قانوني. لذلك تبقى البوابات المحددة `RUNTIME_PENDING` أو `EXTERNAL_PENDING` ولا تُحوّل إلى `VERIFIED` بالاستنتاج.

هذا التقرير هو نقطة البداية المطلوبة قبل الإصلاحات الجديدة. الخطوة التالية هي تثبيت خريطة التكامل ومصفوفة جذور السبب، ثم معالجة كل root cause مشترك في دفعة صغيرة لها اختبار مناسب وCI، دون إعادة فتح إصلاحات أُغلقت إلا إذا كشف التدقيق دليلاً جديداً.
