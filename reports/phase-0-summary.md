# Phase 0 — Repository Safety & Architecture Inventory

Generated by stabilization architect run.

## Repository Snapshot

- Branch: `v0/dawodsideq-2990-61c48abf` (working branch on top of `architecture-refactor`)
- Working tree: clean at start of phase 0
- Total Kotlin source files under `app/src/main/java`: **474**
- Native llama.cpp vendored sources in tree: ~275 C/C++ files (large maintenance burden)
- React/Vite UI shell at repo root: present but `src/` directory is empty (decorative for this branch — not a deliverable)

## Module Density (top directories by file count)

| Path | Files |
|---|---|
| `ui/screens` | 43 (38 distinct `*Screen.kt`) |
| `agent/execution` | 30 |
| `agent/planning` | 22 |
| `agent/subagent` | 14 |
| `accessibility/service` | 13 |
| `agent/decision` | 12 |
| `ai/skills` | 11 |
| `execution/cloud` | 10 |
| `ai/agent` (legacy) | 10 |

## God-Class Hot Spots (lines of code)

| File | LOC |
|---|---|
| `ui/viewmodel/ChatViewModel.kt` | 3,277 |
| `ui/screens/ModelSettingsScreen.kt` | 2,426 |
| `ui/screens/ChatScreen.kt` | 2,313 |
| `ai/LlamaManager.kt` | 1,973 |
| `ui/screens/ExecDiagnosticsScreen.kt` | 980 |
| `ui/screens/SkillBuilderScreen.kt` | 849 |
| `core/VoiceManager.kt` | 761 |
| `ui/screens/ModelLibraryScreen.kt` | 692 |

These four (ChatViewModel, ChatScreen, ModelSettingsScreen, LlamaManager) account for ~10k LOC and represent the highest-leverage refactor surface.

## Navigation Graph

`AiriApp.kt` declares **35 composable routes** but only **5 routes are reachable from the bottom nav**:

```
bottomNavRoutes = { CHAT, SETTINGS, SKILL_MANAGER, AGENT_TASKS, HISTORY }
```

The remaining ~30 routes are reachable only via deep links from settings or other screens, and several are not linked from anywhere visible in the UI. Confirmed unreachable (or only reachable via developer-center side door): `OBSERVABILITY`, `EXEC_DIAGNOSTICS`, `DEBUG_PANEL`, `DEBUG_SCREEN`, `SANDBOX_WORKSPACE`, `WORKSPACE`, `TERMINAL`, `MODEL_PERFORMANCE`, `PERFORMANCE`, `AGENT_TRACE_DETAIL`, `AGENT_LOGS`, `REFERRALS`. (See `phase-0-duplicate-systems.md` and `phase-0-dead-systems.md`.)

## Subsystems Confirmed Present in Source Tree

- Two parallel agent stacks: legacy `ai/agent/*` (`AgentController`, `TaskPlanner`, `TaskExecutor`, `Task.kt`, `TaskStep.kt`, `AgentResult.kt`, plus `trace/` and `background/`) AND modern `agent/*` (`ProductionAgentOrchestrator`, `SubAgentRegistry`, `UnifiedCognitiveLoop`, `TypedPlanGraph`, `execution/task/TaskStep.kt`).
- 12 engines under `agent/decision/`: `AdaptiveBehaviorEngine`, `AdaptiveDecisionEngine`, `BehaviorPolicy`, `ConfidenceScorer`, `DecisionEngine`, `DialogueRhythmEngine`, `EmotionEngine`, `GuardianEngine`, `PatternAggregator`, `RelationshipBoundaryPolicy`, `RiskProvider`, `SuggestionScoreEngine`.
- 4 engines under `agent/adaptation/` (Strategy/Failure/PlannerAdaptation/PlanAdaptationHints).
- 3 reflection components, 3+ learning components, 3 multiagent components.
- 9 sub-agents (most are LLM-delegation shells — see dead-systems report).
- 38 distinct `*Screen.kt` files.

## Existing Internal Reports (preserved)

`docs/reports/` already contains: `DeadSystemsReport.md`, `FinalProductionReport.md`, `LifecycleAudit.md`, `NetworkingAudit.md`, `RuntimeInteractionReport.md`, `StabilityAudit.md`. These are kept as-is. The new `reports/` directory holds the per-phase outputs of this stabilization run.

## Phase-0 Deliverables

- [x] Branch + commit safety verified (working tree clean, push pipeline working).
- [x] Architecture inventory (this file).
- [x] Dead-system report (`phase-0-dead-systems.md`).
- [x] Duplicate-system report (`phase-0-duplicate-systems.md`).
- [x] Risks register (`phase-0-risks.md`).
- [x] Next steps (`phase-0-next-steps.md`).
